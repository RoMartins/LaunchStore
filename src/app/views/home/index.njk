 {% extends 'layout.njk' %}

{% block content %}
<section class="shop container">
    <h2> Últimos Produtos </h2>
        <div class="items">
        
        {% for product in products  %}
        <div class="product">

            <div 
                class="image"
                
                style = "background-image: url({{product.img}})"
            >
            </div>
            {% if product.FormatOldPrice != product.FormatPrice %}
                <div class="old-price">{{product.FormatOldPrice}}</div>
            {% endif %} 

            <p>{{product.FormatPrice}}</p>
            <h4>{{product.name}}</h4>

            <a href="/products/{{product.id}}" class="button">Ver produto</a>
            </div>        
        {% endfor %}
                
        </div>

</section>

{% endblock content %}
